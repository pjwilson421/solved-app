/**
 * Date/time line under history thumbnails in the Create Image / Video history dropdown
 * (`HistoryPanel` caption above the prompt). Maps to `--text-muted` / `text-tx-muted` (#7a8ad4).
 * Chat thread metadata (day separators, swipe-reveal times) uses the same class for parity.
 */
export const HISTORY_THUMB_DATETIME_TEXT_CLASS = "text-tx-muted";
